[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "audit-flow-backend"
version = "0.1.0"
description = "AuditFlow backend"
requires-python = ">=3.11,<3.15"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
